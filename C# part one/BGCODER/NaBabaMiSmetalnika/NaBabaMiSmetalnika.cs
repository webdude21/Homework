namespace NaBabaMiSmetalnika
{
    using System;

    internal class NaBabaMiSmetalnika
    {
        private static void Main()
        {
            const int Height = 8;
            var widht = int.Parse(Console.ReadLine());
            var topcheta = new bool[widht, Height];

            for (var i = 0; i < Height; i++)
            {
                var currentLineOfTopcheta = int.Parse(Console.ReadLine());
            }
        }
    }
}